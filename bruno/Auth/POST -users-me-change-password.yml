info:
  name: POST /users/me/change-password
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/auth/users/me/change-password"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
